# تقرير حالة مشروع Championship OS

**تاريخ التقرير:** 13 أغسطس 2026  
**اسم المشروع:** Championship OS  
**الغرض:** نظام إدارة بطولات رياضات القتال مثل BJJ وMMA، من إنشاء البطولة والتسجيل العام وحتى الميزان، البولات، البراكت، التحكيم، النتائج، وبوابة اللاعب.

## 1. الملخص التنفيذي

تم بناء نسخة MVP عملية من Championship OS باستخدام React وExpress وtRPC وDrizzle، مع نشر الكود على GitHub والنظام على Vercel. النظام يحتوي على رابط تسجيل عام لكل بطولة، تسجيل لاعبين من الإدارة، تصنيف تلقائي حسب النوع والحزام والسن والوزن، تعيين بولة تلقائي، متابعة الدفع وCheck-in والميزان، إنشاء مواجهات تلقائي أو يدوي، واجهة تسجيل نتائج وتايمر رقمي، وبوابة عامة للاعب.

تم إصلاح مشكلة الصفحة البيضاء ومشكلة ملفات JavaScript القديمة في Vercel، وإضافة متغير `VITE_CANONICAL_APP_ORIGIN` في إعدادات Vercel. أصبح زر الدخول من نطاق Vercel يوجّه إلى النطاق المصرّح به OAuth بدل إرسال `redirect_uri` غير مسموح. فحص الإنتاج أكد أن الصفحة الرئيسية تعمل، ورابط التسجيل العام ورابط بوابة اللاعب يعرضان حالات واضحة.

> **الخلاصة المهمة:** التطبيق والكود والنشر يعملان كنسخة MVP، لكن قاعدة التشغيل الفعلية ما زالت مرتبطة بطبقة MySQL/TiDB الحالية من خلال `DATABASE_URL`. تم التحقق من وصول مشروع Supabase، لكن جداول التطبيق لم تُطبّق بعد ولم يتم استبدال Adapter الحالي بـ PostgreSQL.

## 2. الروابط الأساسية

| العنصر | الرابط | الحالة |
|---|---|---|
| الموقع العام على Vercel | [egyptbjj.vercel.app](https://egyptbjj.vercel.app/) | يعمل، وقد يحتاج تحديثًا عاديًا إذا ظهر Cache قديم |
| رابط مع Cache Bust | [فتح النسخة المحدثة](https://egyptbjj.vercel.app/?cachebust=1786640200) | يستخدم عند ظهور صفحة قديمة أو بيضاء |
| النطاق البديل المصرّح لـ OAuth | [champios-haf3fxkp.manus.space](https://champios-haf3fxkp.manus.space/) | يعمل ويُستخدم كـ Canonical OAuth origin |
| GitHub | [github.com/momentarek121/championship-os](https://github.com/momentarek121/championship-os) | الكود مرفوع على فرع `main` |
| صفحة التسجيل العامة | `https://egyptbjj.vercel.app/register/{slug}` | الرابط يتكون من Slug البطولة |
| بوابة اللاعب | `https://egyptbjj.vercel.app/athlete/{slug}` | بحث عام بكود الاعتماد/Accreditation Code |

## 3. التكنولوجيا والبنية

| الطبقة | المستخدم حاليًا |
|---|---|
| الواجهة | React 19 + Vite + Tailwind CSS 4 + shadcn/ui |
| الخادم | Express 4 ضمن Serverless/Vercel |
| API | tRPC 11 مع SuperJSON |
| ORM | Drizzle ORM |
| قاعدة البيانات الحالية | MySQL/TiDB من خلال `DATABASE_URL` |
| قاعدة البيانات المستهدفة | Supabase PostgreSQL |
| تسجيل الدخول | Manus OAuth مع جلسة Cookie |
| الاستضافة العامة | Vercel، مشروع `egyptbjj` |
| التخزين/الخدمات المساعدة | متغيرات Manus Forge والـ runtime المدمجة بالمشروع |
| الاختبارات | Vitest |
| مستودع الكود | GitHub عام على حساب `momentarek121` |

## 4. ما الموجود داخل النظام

### أ. إنشاء البطولة والإعدادات

الإدارة تستطيع إنشاء بطولة وإدخال اسم البطولة، المنظمة، القواعد، إعدادات الميزان، الـ Slug العام، المراحل الأساسية، الحلبات أو Mats، وإعدادات التشغيل. يوجد تدفق إعداد موجه بدل شاشة Dashboard عامة فقط.

الإعداد الحالي يدعم وضع الميزان، هامش السماح، قواعد IBJJF، التصنيفات، Mats، ورابط التسجيل. بعض التفاصيل الإضافية مثل Scale Notes المتقدمة تحتاج توسعة لاحقة إذا أردت استخدامها كحقل مستقل.

### ب. التسجيل العام

كل بطولة يمكن أن تحصل على رابط عام بالشكل التالي:

```text
https://egyptbjj.vercel.app/register/TOURNAMENT_SLUG
```

اللاعب يفتح الرابط بدون تسجيل دخول Vercel، ويدخل بياناته الأساسية، ومنها الاسم، البريد أو الهاتف حسب النموذج، النوع، الحزام، الوزن، وتاريخ الميلاد. تاريخ الميلاد أصبح إلزاميًا حتى لا يدخل لاعب تلقائيًا في فئة Adult بسبب قيمة افتراضية.

بعد الإرسال، يتم عرض نتيجة التسجيل مع الفئة المحسوبة والبولة التلقائية عندما تتوفر بيانات البطولة والتصنيف. الرابط العام لا يحتاج إلى حساب OAuth للاعب.

### ج. التصنيف التلقائي والبولات

التصنيف يعتمد على مجموعة من العوامل:

| عامل التصنيف | الاستخدام |
|---|---|
| تاريخ الميلاد | حساب السن والفئة Juvenile/Adult/Master حسب القواعد |
| النوع | فصل فئات الرجال والسيدات حسب إعداد البطولة |
| الحزام | فصل Beginner/Intermediate/Advanced أو حزام BJJ المقابل |
| الوزن | اختيار فئة الوزن حسب قواعد IBJJF |
| الرياضة والقواعد | دعم قواعد بطولة مخصصة مستقبلًا إلى جانب BJJ |
| ترتيب التسجيل | تثبيت Pool A وPool B والباقي بشكل deterministic |

تم جعل ترتيب التسجيل ثابتًا عبر ترتيب السجلات بالـ ID/ترتيب قاعدة البيانات قبل حساب اسم البولة، حتى لا يتغير اسم البولة عشوائيًا بين قراءة وأخرى. البولات حاليًا طبقة تنظيمية تلقائية تمهّد لإنشاء المواجهات؛ يمكن إضافة قواعد حجم البولة والتوزيع المتقدم لاحقًا.

### د. الدفع وCheck-in والميزان

لوحة الإدارة تحتوي على حالات الدفع، Check-in، وحالة الميزان. الميزان يدعم نمط IBJJF مع هامش سماح قابل للإعداد. يستطيع فريق التنظيم تشغيل اللاعب من مرحلة التسجيل إلى الحضور ثم الوزن وتسجيل النتيجة أو حالة عدم اجتياز الميزان.

النسخة الحالية تحتوي على Hooks تشغيلية لهذه الخطوات داخل Workspace. التوسع الطبيعي التالي هو شاشة يوم البطولة المتخصصة لكل فريق: Registration Staff، Weigh-in Staff، وMat Manager.

### هـ. البراكت والمواجهات

النظام يدعم:

- إنشاء مواجهات تلقائيًا حسب الفئة واللاعبين.
- ترتيب أو Seeds للمواجهات.
- إنشاء مواجهة يدويًا بين لاعبين.
- ربط المباراة بفئة وMat ووقت عند توفرها.
- عرض Queue للمواجهات.
- تسجيل نتيجة المباراة وحالة الفوز/الخسارة.
- واجهة تحكيم داخل مساحة التشغيل.

الموجود حاليًا هو MVP للمواجهات والبراكت. ما زالت هناك توسعات مطلوبة للوصول إلى مستوى Smoothcomp/AJP الكامل، خصوصًا تعديل البراكت بصريًا بعد إنشائه، انتقال الفائز تلقائيًا إلى الجولة التالية، نظام الميداليات النهائي، وتوزيع Mats المتقدم.

### و. التايمر الرقمي

تمت إضافة تايمر رقمي داخل واجهة المباراة، ويشمل:

- مدة قابلة للتحديد.
- Start.
- Pause.
- Reset.
- إظهار الوقت المتبقي والحالة.
- دعم تشغيله أثناء واجهة التحكيم.

التايمر يعمل كحالة واجهة في MVP. إذا كان المطلوب تايمر مركزي يتزامن لحظيًا بين عدة أجهزة Referees، فهذه تحتاج Realtime/WebSocket أو Supabase Realtime بعد إكمال طبقة PostgreSQL.

### ز. بوابة اللاعب

بوابة اللاعب عامة ولا تحتاج لوحة الإدارة. الرابط:

```text
https://egyptbjj.vercel.app/athlete/TOURNAMENT_SLUG
```

اللاعب يستخدم كود الاعتماد لمعرفة حالته، فئته، حالة التسجيل والميزان، المباراة القادمة، الـ Mat، الوقت المجدول، وسجل البراكت المتاح. عند عدم وجود بطولة أو كود صحيح تظهر حالة واضحة بدل شاشة فارغة.

### ح. الصلاحيات

تم وضع حد Admin/Owner على عمليات مساحة التنظيم الحساسة، مثل التسجيل الداخلي، الميزان، إنشاء المواجهات، تغيير حالة المباراة، وتسجيل النتائج. التسجيل العام وبوابة اللاعب يظلان Public. مالك المشروع المعرّف في متغيرات Manus يستطيع تشغيل مساحة التنظيم، بينما المستخدم العادي لا يحصل تلقائيًا على صلاحيات الإدارة.

> الصلاحيات المتقدمة المنفصلة لكل من Referee وRegistration Staff وWeigh-in Staff وMat Manager لم تُفصل بعد إلى Views مستقلة كاملة؛ الموجود حاليًا Owner/Admin Workspace مع Athlete Portal عام.

### ط. Audit Log

التغييرات الحساسة تكتب سجل تدقيق، ومنها إنشاء وتعديل التسجيل، تغييرات الميزان، إنشاء المواجهات، تعديل حالة المباراة، والنتيجة. الهدف هو معرفة من غيّر ماذا ومتى، مع تسجيل القيم قبل وبعد عندما تكون متاحة.

## 5. الربط مع الخدمات الخارجية

### Vercel

المشروع مرتبط بمشروع Vercel اسمه `egyptbjj`. تم تعطيل حماية Vercel Deployment Protection حتى تفتح الروابط العامة بدون Vercel SSO. آخر عملية Redeploy تمت من إعدادات Vercel بعد إضافة متغير Canonical OAuth Origin.

متغير الإنتاج الجديد:

```text
VITE_CANONICAL_APP_ORIGIN=https://champios-haf3fxkp.manus.space
```

وظيفته أن أي زائر يفتح Vercel ويضغط Sign in يتم تحويله إلى النطاق المصرّح به قبل بدء Manus OAuth، لأن `egyptbjj.vercel.app` غير مسجل حاليًا كنطاق Redirect مسموح في تطبيق OAuth.

### GitHub

الكود موجود على:

```text
https://github.com/momentarek121/championship-os
```

الفرع الأساسي هو `main`. تم دفع آخر إصلاح OAuth تحت commit `50a6eca`، وأنشأ Vercel Deployment إنتاجي جديد بعد إضافة المتغير من لوحة Vercel.

### Manus OAuth

تسجيل الدخول ليس Supabase Auth حاليًا؛ هو Manus OAuth. المسار العام هو:

```text
الموقع → /api/oauth/login أو startLogin → manus.im/app-auth → /api/oauth/callback → جلسة Cookie → Organizer Workspace
```

النطاق المصرح حاليًا هو نطاق Manus managed:

```text
https://champios-haf3fxkp.manus.space
```

لذلك زر Vercel يقوم بالتحويل إلى النطاق المصرح قبل فتح OAuth. إكمال تسجيل دخول حقيقي إلى Workspace يحتاج حساب المستخدم نفسه في المتصفح، ولا يتم تنفيذ كلمة المرور أو Google login بالنيابة عنه.

### Supabase

تم ربط متغيرات Supabase في Vercel واختبار الوصول إلى REST root. المتغيرات هي:

| المتغير | وظيفته |
|---|---|
| `VITE_SUPABASE_URL` | رابط مشروع Supabase |
| `VITE_SUPABASE_ANON_KEY` | مفتاح الواجهة العامة |
| `SUPABASE_SERVICE_ROLE_KEY` | مفتاح السيرفر السري |

لكن وجود هذه المتغيرات لا يعني أن التطبيق أصبح PostgreSQL تلقائيًا. الكود الحالي ما زال يستخدم Drizzle Adapter مبنيًا على MySQL/TiDB من خلال `DATABASE_URL`.

المشكلة الحالية في Supabase ليست في الرابط؛ REST root يستجيب، لكن فحص `public.tournaments` أعاد 404 لأن `supabase/schema.sql` لم يُنفّذ داخل SQL Editor. كذلك لا يجوز اعتبار Service Role Key الذي تمت مشاركته في المحادثة آمنًا؛ يجب تدويره من Supabase ثم تحديثه في Vercel.

## 6. الاختبارات التي تم تنفيذها

آخر تشغيل كامل أظهر:

```text
7 test files passed
11 tests passed
TypeScript check passed
Production build passed
```

التغطية الحالية تشمل:

- تسجيل الدخول وبناء OAuth URL.
- Canonical origin configuration.
- تسجيل الخروج.
- حساب السن والتصنيف.
- أسماء البولات الثابتة.
- Athlete Portal واختيار المباراة التالية.
- إعداد Supabase وصحة REST root.

تم أيضًا اختبار الإنتاج بصريًا على:

- `/` للصفحة الرئيسية.
- `/register/demo`، وظهرت حالة `Registration link not found` بشكل صحيح للـ Slug التجريبي غير الموجود.
- `/athlete/demo`، وظهرت حالة البحث العامة بشكل واضح.
- زر Sign in على Vercel، وتم التأكد من تحويله إلى النطاق المصرّح بدلاً من فشل `invalid redirect_uri`.

## 7. ما هو جاهز وما هو غير مكتمل

| المجال | الحالة الحالية |
|---|---|
| GitHub repository | جاهز ومرفوع |
| Vercel deployment | منشور وReady |
| Public homepage | يعمل |
| Public registration route | يعمل ويعرض حالات واضحة |
| Athlete portal | يعمل كـ MVP |
| Login initiation | تم إصلاح تحويل النطاق، وإكمال login الحقيقي يحتاج حساب المستخدم |
| Tournament creation | جاهز |
| Category assignment | جاهز مع تاريخ ميلاد إلزامي |
| Automatic pools | جاهز بشكل deterministic |
| Check-in/payment/weigh-in hooks | موجودة في Workspace |
| Digital timer | جاهز كـ MVP |
| Automatic/manual matches | موجودة |
| Audit logs | موجودة للتغييرات الحساسة |
| Role separation | Owner/Admin موجود؛ Views منفصلة لكل دور تحتاج توسعة |
| Supabase REST access | تم التحقق من الوصول |
| Supabase tables | لم تُطبّق بعد في المشروع المستهدف |
| PostgreSQL Adapter | لم يتم استبدال MySQL/TiDB بعد |
| Full automatic winner advancement | غير مكتمل بالكامل |
| Full visual bracket editing | غير مكتمل بالكامل |
| Live multi-device timer sync | غير موجود بعد |
| Real end-to-end authenticated walkthrough | يحتاج تسجيل دخول المستخدم |

## 8. الخطوات المطلوبة لإغلاق Supabase فعليًا

1. افتح مشروع Supabase الصحيح.
2. ادخل إلى **SQL Editor**.
3. نفّذ الملف `supabase/schema.sql` بعد مراجعته.
4. تأكد من ظهور `public.tournaments` وباقي الجداول في Table Editor.
5. دوّر Secret/Service Role key الذي تمت مشاركته في المحادثة.
6. حدّث `SUPABASE_SERVICE_ROLE_KEY` في Vercel بالمفتاح الجديد.
7. بعد ذلك يتم تنفيذ ترحيل Adapter من Drizzle MySQL إلى Drizzle PostgreSQL أو طبقة Supabase مناسبة، ثم اختبار حفظ لاعب حقيقي واسترجاعه.

## 9. التقييم النهائي

Championship OS حاليًا نسخة MVP قوية لإدارة بطولة صغيرة أو متوسطة من Workspace واحد: إنشاء البطولة، رابط التسجيل، تصنيف اللاعب، البولة، الميزان، المواجهات، التحكيم، التايمر، والبوابة العامة. الرابط العام والنشر والكود على GitHub جاهزون.

الجزء الذي لا ينبغي اعتباره منتهيًا هو Supabase كقاعدة تشغيل نهائية؛ تم تجهيز المتغيرات والـ schema المرجعي والتحقق من الوصول، لكن التطبيق ما زال يعتمد على MySQL/TiDB حتى يتم تنفيذ الجداول وترحيل Adapter. كذلك الوصول إلى مستوى Smoothcomp/AJP الكامل يحتاج Realtime، فصل Views حسب الأدوار، انتقال فائز تلقائي، ومحرر Bracket متقدم.

### الروابط النهائية الموصى بها

- **للموقع:** https://egyptbjj.vercel.app/
- **إذا ظهر Cache قديم:** https://egyptbjj.vercel.app/?cachebust=1786640200
- **النطاق البديل المصرح للدخول:** https://champios-haf3fxkp.manus.space/
- **GitHub:** https://github.com/momentarek121/championship-os

