import { Link, useRoute } from "wouter";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Languages, ShieldCheck, Swords } from "lucide-react";
import { useLanguage } from "@/contexts/LanguageContext";

export default function RegistrationRules() {
  const [, params] = useRoute("/register/:slug/rules");
  const slug = params?.slug ?? "";
  const event = trpc.publicRegistration.getBySlug.useQuery({ slug }, { enabled: Boolean(slug) });
  const { language, setLanguage, t } = useLanguage();
  if (event.isLoading) return <div className="grid min-h-screen place-items-center bg-[#07111f] text-white">Loading rules…</div>;
  if (!event.data) return <div className="grid min-h-screen place-items-center bg-[#07111f] p-6 text-center text-white"><h1 className="text-3xl font-black">Registration link not found</h1></div>;
  return <div className="min-h-screen bg-[#07111f] px-4 py-8 text-white md:py-14"><div className="mx-auto max-w-4xl"><header className="mb-8 flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-[#d7ff54] text-[#07111f]"><Swords className="h-6 w-6" /></div><div><p className="font-black">{event.data.organizationName}</p><p className="text-sm text-slate-400">{event.data.name}</p></div></div><Button variant="outline" className="border-white/20 bg-transparent text-white" onClick={() => setLanguage(language === "en" ? "ar" : "en")}><Languages className="mr-2 h-4 w-4" />{t("language")}</Button></header><div className="mb-8 max-w-2xl"><Badge className="bg-[#d7ff54] text-[#07111f]">Before registration</Badge><h1 className="mt-4 text-4xl font-black tracking-tight md:text-6xl">Choose your competition mode with confidence.</h1><p className="mt-4 text-lg leading-8 text-slate-300">Read the GI and No-Gi basics, then continue to the registration form. Your selection determines your category and pool.</p></div><div className="grid gap-5 md:grid-cols-2"><Card className="border-0 bg-white text-slate-900 shadow-2xl"><CardHeader><CardTitle className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-violet-600" /> GI rules</CardTitle></CardHeader><CardContent className="space-y-3 text-sm leading-7 text-slate-600"><p>Wear a clean, regulation-appropriate gi with a belt. The organizer may inspect the gi before the match.</p><p>Weight is checked according to the tournament settings. Arrive on time for check-in and weigh-in.</p><p>Choose GI only if you want to compete in the gi category and its dedicated pool.</p></CardContent></Card><Card className="border-0 bg-white text-slate-900 shadow-2xl"><CardHeader><CardTitle className="flex items-center gap-2"><Swords className="h-5 w-5 text-emerald-600" /> No-Gi rules</CardTitle></CardHeader><CardContent className="space-y-3 text-sm leading-7 text-slate-600"><p>Wear approved rashguard and shorts or spats suitable for No-Gi competition. No traditional gi jacket is used.</p><p>Weight is checked according to the tournament settings. Bring the exact expected weight to the official scale.</p><p>Choose No-Gi only if you want the No-Gi category and its dedicated pool.</p></CardContent></Card></div><Card className="mt-5 border-white/10 bg-white/[.08] text-white"><CardContent className="p-6"><h2 className="text-xl font-black">Both modes</h2><p className="mt-2 leading-7 text-slate-300">Select Both only when you intentionally want the organizer to register you in the combined GI + No-Gi category according to the event setup. This is not the same as selecting GI and No-Gi separately.</p></CardContent></Card><div className="mt-8 flex flex-wrap gap-3"><Button asChild className="bg-[#d7ff54] text-[#07111f]"><Link href={`/register/${slug}`}>Continue to registration</Link></Button><Button asChild variant="outline" className="border-white/20 bg-transparent text-white"><Link href={`/event/${slug}/participants`}>View participants</Link></Button></div></div></div>;
}
