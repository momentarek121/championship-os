import { useEffect, useState } from "react";
import { Link } from "wouter";
import { ArrowLeft, Pause, Play, RotateCcw } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const demoMatches = [
  { number: 1, division: "Kids · Boys · 30 KG", mat: "Mat 1", a: "Adam Demo", b: "Omar Demo", scoreA: 4, scoreB: 0, status: "live" },
  { number: 2, division: "Girls · Youth · 45 KG", mat: "Mat 2", a: "Lina Demo", b: "Mariam Demo", scoreA: 0, scoreB: 0, status: "queued" },
  { number: 3, division: "Men · Adult · 76 KG", mat: "Mat 3", a: "Mostafa Demo", b: "Amr Demo", scoreA: 8, scoreB: 6, status: "finished" },
];

export default function DemoReferee() {
  const [seconds, setSeconds] = useState(5 * 60);
  const [running, setRunning] = useState(false);
  useEffect(() => { if (!running || seconds <= 0) return; const timer = window.setInterval(() => setSeconds(value => Math.max(0, value - 1)), 1000); return () => window.clearInterval(timer); }, [running, seconds]);
  const clock = `${String(Math.floor(seconds / 60)).padStart(2, "0")}:${String(seconds % 60).padStart(2, "0")}`;
  return <div className="min-h-screen bg-[#07111f] px-4 py-8 text-white sm:px-8"><div className="mx-auto max-w-6xl"><Link href="/" className="inline-flex items-center gap-2 text-sm text-slate-300 hover:text-[#d7ff54]"><ArrowLeft className="h-4 w-4" /> Back to Championship OS</Link><div className="mt-8 flex flex-col gap-5 border-b border-white/10 pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.3em] text-[#d7ff54]">Demo referee desk · no production data</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-6xl">Call the next match.</h1><p className="mt-3 max-w-2xl text-slate-300">Test the referee view, score display, match queue, mats, and timer with synthetic athletes before opening real scoring.</p></div><Badge className="w-fit bg-white/10 px-4 py-2 text-slate-200">Read-only demo</Badge></div><div className="mt-8 grid gap-6 lg:grid-cols-[.7fr_1.3fr]"><Card className="border-0 bg-white text-slate-900"><CardHeader><CardTitle>Digital match timer</CardTitle><p className="text-sm text-slate-500">Local demo timer · does not write a match result.</p></CardHeader><CardContent><div className="rounded-2xl bg-[#07111f] p-8 text-center text-6xl font-black tabular-nums text-[#d7ff54]">{clock}</div><div className="mt-4 flex gap-2"><Button onClick={() => setRunning(value => !value)} className="flex-1 bg-[#07111f]"><span className="mr-2">{running ? <Pause className="h-4 w-4" /> : <Play className="h-4 w-4" />}</span>{running ? "Pause" : "Start"}</Button><Button onClick={() => { setRunning(false); setSeconds(5 * 60); }} variant="outline" size="icon"><RotateCcw className="h-4 w-4" /></Button></div></CardContent></Card><Card className="border-0 bg-white text-slate-900"><CardHeader><CardTitle>Live match queue</CardTitle><p className="text-sm text-slate-500">Synthetic queue states for tournament-day training.</p></CardHeader><CardContent className="space-y-3">{demoMatches.map(match => <div key={match.number} className="rounded-2xl border border-slate-200 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="font-bold">Match {match.number} · {match.division}</p><p className="text-xs text-slate-500">{match.mat}</p></div><Badge className={match.status === "live" ? "bg-red-100 text-red-800" : match.status === "finished" ? "bg-emerald-100 text-emerald-800" : "bg-slate-100 text-slate-700"}>{match.status}</Badge></div><div className="mt-3 grid grid-cols-[1fr_auto] gap-2 text-sm"><span className="rounded-lg bg-slate-50 p-3 font-semibold">{match.a}</span><span className="rounded-lg bg-slate-50 p-3 font-black">{match.scoreA}</span><span className="rounded-lg bg-slate-50 p-3 font-semibold">{match.b}</span><span className="rounded-lg bg-slate-50 p-3 font-black">{match.scoreB}</span></div><Button className="mt-3 w-full" variant="outline" disabled>{match.status === "live" ? "Demo scoring controls disabled" : "Read-only demo match"}</Button></div>)}</CardContent></Card></div><div className="mt-8 flex flex-wrap gap-3"><Button asChild className="bg-[#d7ff54] text-[#07111f]"><Link href="/demo/brackets">Open demo brackets</Link></Button><Button asChild variant="outline" className="border-white/20 bg-transparent text-white"><Link href="/event/demo-live/participants">Open demo participants</Link></Button></div></div></div>;
}
